### Quick Questions


# What is the PyCharm shortcut key for commenting selected line/s of code?
Ctrl + / (Windows)

# What is the single most important design principle for designing functions?
Functions should do one thing well

# What is the Python keyword for returning a value from a function?
return

# What are arguments and parameters? What is the difference between them?
Parameters are variables defined in a function
Arguments are the values passed into the function

# What is the Python function call for generating a random number between 1 and 100?
random.randint(1, 100)

# Name 4 functions we've used in the subject so far (functions we've "called").
print()
input()
len()
range()

# How many grams of freshly-ground specialty coffee did Lindsay make his coffee with in the lecture video?
18 grams


### Good Names

# determine a subject grade based on a given total score
determine_grade

# convert currency from USD to AUD
convert_usd_to_aud

# print a report
print_report

# calculate the average of a list of numbers
calculate_average

# determine if a number is even
is_even

# get a user's salary, making sure that it is not negative
get_valid_salary